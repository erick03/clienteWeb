@model IEnumerable<quiz_web.Models.Course>
@{
    ViewBag.Title = "Asociaciones";
    Layout = "~/Views/Shared/_BootstrapLayout.cshtml";
}

<h2 class="text-center">Course information</h2>
<br />
@foreach (var cour in ViewBag.infoCourse)
{
    <strong class="text-warning">cour.name</strong>
}
<br />
@foreach (var item2 in ViewBag.professorsCourse)
{
    <h3>Profesor course: </h3>
    if(@item2.name != "")
    {
        <strong class="text-left text-success ">@item2.name @item2.lastname </strong>
    }else
    {
        <strong class="text-left text-error">unassigned </strong>
    }
}
<br/>
<h2>Students course</h2>
<table class ="table table-striped table-bordered">
    <tr>
        <th>
            UserName
        </th>
        <th>
            Email
        </th>
        <th>
            Identification
        </th>
        <th>
            Name
        </th>
        <th>
            LastName
        </th>
    </tr>
    @foreach (var item in ViewBag.studentsCourse)
    {
        <tr>
            <td>
                @item.username
            </td>
            <td>
                @item.email
            </td>
            <td>
                @item.identification
            </td>
            <td>
                @item.name
            </td>
            <td>
                @item.lastname
            </td>
            <td>
            @Html.ActionLink("Delete", "DeleteStudentCourse",new { id = item.ID },new { @class = "btn btn-danger btn-mini" })
            </td>
        </tr>
    }
</table>
<h2>New students</h2>
<table class ="table table-striped table-bordered">
    <tr>
        <th>
            UserName
        </th>
        <th>
            Email
        </th>
        <th>
            Identification
        </th>
        <th>
            Name
        </th>
        <th>
            LastName
        </th>
    </tr>
    @foreach (var itemNs in ViewBag.notStudentCourse)
    {
        <tr>
            <td>
                @itemNs.username
            </td>
            <td>
                @itemNs.email
            </td>
            <td>
                @itemNs.identification
            </td>
            <td>
                @itemNs.name
            </td>
            <td>
                @itemNs.lastname
            </td>
            <td>
            @Html.ActionLink("Add to course", "DeleteStudentCourse",new { id = itemNs.ID },new { @class = "btn btn-success btn-mini" })
            </td>
        </tr>
    }
</table>
